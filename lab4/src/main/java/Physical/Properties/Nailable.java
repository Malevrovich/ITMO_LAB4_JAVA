package Physical.Properties;

import Physical.PhysicalObject;
import Physical.Position.PositionEngine;

public interface Nailable {
    void makeNailed(PhysicalObject dst, PositionEngine engine);
}
